<Window x:Class="MechwarriorVRLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MechWarrior VR Launcher"
        Height="380"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Icon="assets/Logo.png">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Hero Image -->
        <Image Grid.Row="0"
               Source="assets/Hero.png"
               Stretch="Uniform"
               MaxHeight="150"
               Margin="0,0,0,30"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <!-- Main Action Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <Button x:Name="OpenInstallerButton"
                        Content="Setup"
                        Click="OpenInstallerButton_Click"
                        MinWidth="180"
                        Height="50"
                        FontSize="14"
                        Margin="0,0,15,0"
                        Padding="15,10"/>
                <Button x:Name="OpenLaunchButton"
                        Content="Launch MW5 VR"
                        Click="OpenLaunchButton_Click"
                        MinWidth="180"
                        Height="50"
                        FontSize="14"
                        Padding="15,10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
